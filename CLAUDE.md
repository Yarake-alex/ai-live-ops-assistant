# CLAUDE.md — AI 直播运营助手 MVP 项目上下文

## 项目概况

- AI 直播运营助手 MVP：面向直播电商运营场景的 AI 演示系统（V1 直播运营 MVP → V2 资料检索增强 + 开播准备 → V3 问题洞察 + 本地快答 → V4 问题驱动的资料补齐与话术优化）。
- 后端：FastAPI + SQLAlchemy（SQLite 本地 / PostgreSQL 生产）；测试：pytest（全量基线 389 passed, 1 warning）。

## V5 Vue3 迁移上下文（2026-08 完成，已切换正式入口）

- **V5 目标**：把旧 `static/index.html` 单文件前端逐步迁移为 Vue3 + Vite 组件化前端。
- **当前状态：功能迁移已完成，Vue3 前端已切换为正式演示入口**。Vue3 前端位于 `frontend/`（仅 vue / vite / @vitejs/plugin-vue 依赖，无状态库、无 UI 框架）。
- **已覆盖旧页面全部能力**：登录、会话恢复、401 自动回登录页、退出登录、修改密码；运营工作台；商品列表、搜索、筛选、新增、编辑、删除、CSV 导入导出；商品资料基础信息、资料完整度、资料文档列表、资料文档上传、片段预览、删除、重新整理资料；开播准备概览条；商品资料问答、本地快答、问题洞察、运营建议、直播话术、直播复盘；直播素材库列表、搜索、上传确认、片段预览、删除、清空、重新整理、资料问答；评论助手；用户管理（入口仅管理员可见）。
- **未改动**：后端接口路径、接口返回结构、数据库模型、`.env`。`app/main.py` 仅做最小静态挂载调整：`/` 返回 `frontend/dist/index.html`（dist 不提交 Git，未构建时自动回退旧页面），`/assets` 挂载 dist 资源，新增 `/legacy` 回退路由；Dockerfile 增加 node 多阶段构建。
- **V5 最终验证结果**：`frontend/` npm run build 通过；后端全量 pytest 通过（389 passed, 1 warning）；关键代理链路烟测通过。
- **最新 V5 文档收尾提交**：`6500eaa docs: finalize v5 vue migration notes`。
- **旧 `static/index.html` 保留为回退入口**（`/legacy`、`/static/index.html`），不删除、不降级。
- **入口已切换**：Vue3 前端为默认演示入口（`/`）；真实数据端到端走查仍待可登录环境完成（本机 `.env` 密码非演示默认值，按约束不读取 `.env`，此前只能验证到未登录 401 链路）。

## 已接受的设计取舍

- 旧商品资料页是 Tabs；Vue3 采用纵向卡片堆叠，功能一致但交互形态不同——已接受，不恢复旧 Tabs。
- 旧评论助手生成回复后会跨页刷新商品问题洞察 / 运营建议；Vue3 页面相互独立，暂不做跨页联动——已接受，属可选优化。

## 下次继续时的优先事项

1. **继续 V6 UI 优化**（进行中，进度见 `docs/v6-ui-plan.md` 第五节执行进度）：下一阶段为**阶段 3 运营工作台**（提交信息 `style: polish operations dashboard`），之后依次为阶段 4 商品资料页、阶段 5 素材库/评论助手/用户管理、阶段 6 登录页、阶段 7 响应式/可访问性/最终验证。**工作节奏：每个阶段完成后汇报，等用户检查确认再做下一阶段**（用户明确要求分批次执行）。
2. 在可登录环境完成 Vue3 真实数据完整走查（登录 → 各页面操作 → 退出）。
3. 若走查发现回归，评估修复或临时回退旧入口；走查通过后再规划旧 `static/index.html` 的保留、降级或后续删除方案；**删除必须单独确认，不能直接删**。

## 安全边界

- 不读取、不修改 `.env`；不提交 API Key / SECRET / PASSWORD / TOKEN / 数据库文件。
- 禁止批量删除文件或目录；删除必须逐个显式确认。
