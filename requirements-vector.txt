# SQLite + ChromaDB vector search
# Install: pip install -r requirements-vector.txt
chromadb>=0.5.0,<2.0
# Python 3.9 compat: tenacity>=9.0 requires typing.ParamSpec (Python 3.10+)
tenacity>=8.0,<9.0
